# ============================================
# Market Risk Engine - Environment Template
# ============================================

# GCP Project ID (BigQuery project)
GCP_PROJECT_ID=your-gcp-project-id

# BigQuery dataset name
BQ_DATASET=your_bigquery_dataset

# Environment flag (dev / prod)
ENV=dev

# Optional overrides (defaults shown below)
FEATURES_LATEST_VIEW=features_latest_v3
TARGETS_LATEST_VIEW=targets_latest_v3
DEFAULT_FEATURE_VERSION=v2
DEFAULT_TARGET_VERSION=v2